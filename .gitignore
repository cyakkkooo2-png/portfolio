node_modules/
# dist/  — included in Railway deploy
.env
*.log
.DS_Store
Thumbs.db
ngrok*
tunnel_url.txt

# User data (auto-created by server on first run)
server/data/

# User uploads (stored on cloud disk, not in git)
server/uploads/

# Legacy repository videos (production videos live on Tencent Cloud)
storage/videos/
cloudflared.exe
cf_output.txt
