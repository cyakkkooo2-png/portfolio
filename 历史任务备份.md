# 历史任务备份

这份文件用于列出已经备份过的 Codex 任务/网站版本。以后用户说“查看历史任务”时，优先读取本文件并按时间列出。

## 作品集1

- 备份名称：`作品集1`
- 备份时间：2026-07-15
- Git commit：`cdc58c3`
- Git tag：`作品集1`
- GitHub 仓库：https://github.com/cyakkkooo2-png/portfolio
- 网站地址：https://ccyspace.icu/
- 状态：已推送 GitHub，并已部署到 Railway

### 任务摘要

本阶段完成了 CCY SPACE 个人作品集网站的主要可用版本。网站包含首页、作品展示、关于我、联系合作、后台上传与网站设置等功能。后台支持视频、图片、文章、简历、关于我图片上传；前台支持作品弹窗浏览、外部链接视频站内尝试播放、文章正文展示、作品分类与精选展示。

### 关键决策

- 作品区标题从“精选作品”改为“部分作品”。
- “部分作品”和其他大标题使用中文衬线展示风格，保持黑色 + 橙色强调。
- 默认展示“精选”，精选数量为前 9 个作品。
- 首页排序采用登录后点击移动：先点要移动的作品，再点目标位置。
- 批量上传视频时，标题默认使用视频文件名。
- 未上传视频封面时，系统尝试自动截取视频第 1 秒画面作为封面。
- 上传文件主要存储在 Railway `/data`，迁移服务器时需要单独备份。

### 恢复方法

```powershell
git fetch --tags
git checkout 作品集1
```

如需继续开发：

```powershell
git checkout -b restore-作品集1 作品集1
```

## 作版本4

- 备份名称：`作版本4`
- 备份时间：2026-07-20 12:25:54 +08:00
- Git commit：`a5ab8d7`（网站代码版本）
- Git tag：`作版本4`
- GitHub 仓库：https://github.com/cyakkkooo2-png/portfolio
- 网站地址：https://ccyspace.icu/
- 状态：当前代码已推送 GitHub，并已部署到 Railway

### 任务摘要

本阶段继续打磨 CCY SPACE 主页作品区与后台管理体验。作品区从原来的“标题压在封面上”改为更像视频站的上图下文卡片布局，让封面更亮堂、标题更清晰；同时新增登录后主页作品隐藏/显示、批量选择、全选/全不选、批量隐藏/显示等管理能力。

### 关键决策

- 主页作品卡片采用“封面在上、标题在下”的布局，减少封面暗角。
- 作品标题参考视频站风格，使用深蓝黑、现代中文黑体体系、半粗字重。
- 登录后普通浏览状态下，左上角显示“隐藏/显示”，右上角显示选择按钮。
- 开启排序模式时隐藏“隐藏/显示”和选择按钮，只保留“点击移动”，避免误触。
- 新增“全选当前 / 全不选”，并支持批量隐藏、批量显示。
- 隐藏作品对游客不可见；登录后仍可看到并恢复显示。
- 新增 `.railwayignore`，部署时排除本地依赖、构建产物和临时上传目录，降低 Railway 上传失败概率。

### 恢复方法

```powershell
git fetch --tags
git checkout 作版本4
```

如需继续开发：

```powershell
git checkout -b restore-作版本4 作版本4
```

## 腾讯云稳定版（2026-08-25）

- 备份名称：`腾讯云稳定版（2026-08-25）`
- 备份时间：2026-08-25 16:48:33 +08:00
- Git commit：`ab6f8f2`
- Git tag：无（本次未指定标签名）
- GitHub 仓库：https://github.com/cyakkkooo2-png/portfolio
- 网站地址：https://ccyspace.icu/
- 状态：腾讯云生产环境运行中，HTTPS、GitHub 自动部署和持久化视频上传已启用

### 任务摘要

本阶段为 CCY SPACE 增加作品多选、全选/全不选、批量显示、批量隐藏和批量删除，并把生产环境从 Railway 迁移到腾讯云轻量应用服务器。网站已启用 Let's Encrypt HTTPS，HTTP 会自动跳转到 HTTPS；服务器会定时检查 GitHub `master` 并自动部署新提交。

视频上传已从“必须配置 GitHub Token 或腾讯云 COS”调整为允许保存到腾讯云持久化目录。排障时发现服务器同时存在 ubuntu 与 root 两套 PM2，旧进程占用了 3001 端口并继续返回 Railway 时代的错误；现已停止旧进程，统一由 root PM2 运行新版本。

### 关键决策

- 腾讯云是当前生产环境，Railway 仅保留为历史信息。
- 网站数据和上传文件保存在 `/var/www/ccyspace-data/`，不提交 GitHub。
- 视频本地持久化通过 `ALLOW_LOCAL_VIDEO_STORAGE=true` 启用。
- 线上只保留 root 用户的 PM2，避免两个 PM2 同时争用 3001 端口。
- HTTPS 证书覆盖 `ccyspace.icu` 和 `www.ccyspace.icu`，Certbot 自动续期演练已通过。
- GitHub 自动部署每 2 分钟检查一次 `master`，部署前保留本地差异备份。
- `client/dist/`、依赖目录、缓存、上传文件和密钥不进入 GitHub 备份。

### 恢复方法

```powershell
git fetch origin
git checkout -b restore-tencent-2026-08-25 ab6f8f2
```

恢复服务器时还需单独恢复 `/var/www/ccyspace-data/`；GitHub 备份不包含用户上传的视频、图片和网站数据。
